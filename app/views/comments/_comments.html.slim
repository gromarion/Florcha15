- @comments.each do |c|
  .row
    .large-12.columns
      .row
        .large-12.columns.small-3.message
          h1.small
            = c.username
          p.italic
            = c.text
          span
            = "#{t('comment.ago')} #{time_ago_in_words(c.created_at)}"
  br